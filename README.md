# CAMPSTER

캠핑장과 캠핑 장비를 추천하는 챗봇 서비스입니다. 2022년 학교 수업에서 6인 팀 프로젝트로 진행했으며, 한국어 챗봇 프레임워크 [KoChat](https://github.com/gusdnd852/kochat)을 기반으로 제작했습니다.

> 이 저장소는 당시 작업 이력을 보존하면서 포트폴리오 열람에 필요한 설명과 프론트엔드 코드를 정리한 버전입니다.

## 주요 기능

- 지역을 기준으로 캠핑장 추천
- 원하는 분위기와 조건을 조합한 테마별 캠핑장 추천
- 텐트, 침낭, 조명 등 품목별 캠핑 장비 추천
- 버튼 선택과 텍스트 입력을 함께 사용하는 대화형 UI
- 홈, 챗봇, 커뮤니티 화면을 이동하는 하단 내비게이션

### 현재 확인할 수 있는 프론트엔드 데모

- 닉네임 입력 후 지역·테마·장비 추천 방식 선택
- 최대 3개의 캠핑 테마 선택
- 카테고리별 캠핑 장비 선택
- 백엔드 미연결 상태를 설명하는 데모 응답 카드
- 장비 거래·캠핑 후기·캠핑 추천 게시글로 구성한 커뮤니티 프로토타입
- 커뮤니티 카테고리별 예시 게시글 필터링

프론트엔드 데모에 표시되는 게시글과 응답은 화면 흐름을 보여주기 위한 예시이며 실제 데이터가 아닙니다.

## 담당 역할

6인 팀에서 다른 팀원 1명과 함께 **챗봇 프론트엔드**를 담당했습니다.

- HTML 기반 챗봇 화면 및 하단 내비게이션 구성
- CSS를 활용한 채팅 메시지, 입력창, 선택 버튼 UI 구현
- JavaScript와 jQuery를 활용한 메시지 출력 및 사용자 입력 처리
- 지역·테마·장비 추천 흐름에 맞춘 선택형 인터랙션 구현
- 백엔드 API 응답을 채팅 화면에 표시하는 프론트엔드 연동

챗봇 모델과 자연어 처리, 서버 및 백엔드 API 구현은 담당하지 않았습니다.

### 기여 기록 안내

프론트엔드 코드는 팀원과 함께 작성한 뒤 팀원 한 명의 GitHub 계정으로 일괄 업로드했습니다. 따라서 Git 커밋 작성자와 기여자 통계만으로 개인별 작업을 구분하기 어렵습니다. 위 담당 역할은 당시 제가 실제로 공동 작업한 범위만 기재했습니다.

## 기술 구성

| 구분 | 기술 |
| --- | --- |
| 담당 영역 | HTML, CSS, JavaScript, jQuery, Bootstrap |
| 프로젝트 구성 | Python, Flask, KoChat |
| 챗봇 처리 | KoChat 기반 의도 분류 및 개체명 인식 |
| UI 글꼴 | Freesentation |

`Python`, `Flask`, `KoChat`은 전체 프로젝트에서 사용한 기술이며, 제 담당 영역은 프론트엔드였습니다.

## 프로젝트 구조

```text
demo/
├── templates/          # 챗봇, 홈, 커뮤니티 화면
├── static/
│   ├── css/main.css    # 프로젝트 UI 스타일
│   ├── js/main.js      # 채팅 및 선택형 인터랙션
│   └── image/          # 화면 이미지 리소스
├── application.py     # Flask 애플리케이션 진입점
├── scenario.py        # 챗봇 시나리오 정의
└── data/               # 의도·개체 학습 데이터
```

## 브랜치 기록

당시 기능별 브랜치에서 작업했으며 일부 작업은 기본 브랜치에 병합되지 않은 상태로 남았습니다.

- `main`: KoChat 기반 초기 코드
- `hm`: 검색 API 관련 작업
- `sb`: 챗봇 프론트엔드 공동 작업
- `song`: 별도 저장소 구조에서 진행한 통합 작업
- `portfolio`: `sb`를 기준으로 만든 포트폴리오 정리본

과거 커밋의 작성자와 날짜는 수정하지 않았으며, 포트폴리오를 위한 변경은 기존 작업과 구분하여 기록합니다.

## 프론트엔드 데모 실행

Python 3가 설치되어 있다면 별도 패키지 설치 없이 정적 화면을 확인할 수 있습니다.

```powershell
cd 2022capstone
py -m http.server 8000 --bind 127.0.0.1
```

macOS 또는 Linux에서는 `py` 대신 `python3`를 사용합니다.

```bash
python3 -m http.server 8000 --bind 127.0.0.1
```

서버 실행 후 다음 주소로 접속합니다.

| 화면 | URL |
| --- | --- |
| 홈 | `http://127.0.0.1:8000/demo/templates/home.html` |
| 챗봇 | `http://127.0.0.1:8000/demo/templates/index.html` |
| 커뮤니티 | `http://127.0.0.1:8000/demo/templates/community.html` |

서버는 실행 중인 터미널에서 `Ctrl+C`를 눌러 종료할 수 있습니다.

## 백엔드 실행 관련 안내

프로젝트 당시 챗봇 백엔드는 AWS 서버에 배포하고 GPU를 사용해 실행했습니다. 현재 저장소만으로는 당시 서버 환경과 학습 모델, 외부 검색 API 설정을 그대로 재현하기 어렵습니다.

또한 2022년 당시의 Python 라이브러리 버전을 사용하므로 전체 챗봇 기능을 다시 실행하려면 다음 항목을 별도로 준비해야 합니다.

- `requirements.txt`에 명시된 구버전 의존성
- KoChat 학습 모델과 실행 환경
- 외부 검색 API 키 및 설정
- Flask 기반 챗봇 API 서버

이 때문에 포트폴리오 정리본은 프론트엔드 데모 모드를 기본으로 사용합니다. 별도의 API 주소를 설정하지 않으면 네트워크 요청을 보내지 않고, 추천 요청이 전달되는 UI 영역만 예시로 보여줍니다.

## 포트폴리오 정리 내역

2026년 포트폴리오 기록을 위해 다음 내용을 정리합니다.

- 프로젝트와 개인 역할을 설명하는 README 작성
- 깨진 프론트엔드 리소스 경로와 문구 수정
- HTML 구조와 접근성 문구 정리
- 데스크톱·모바일 반응형 채팅 UI 보완
- 백엔드 없이 확인 가능한 프론트엔드 데모 응답 추가
- 커뮤니티 게시판 프로토타입 및 카테고리 필터 추가
- JavaScript 입력 처리와 API 주소 구성 정리

프로젝트의 핵심 기능과 과거 기여 기록은 유지합니다.

## 기반 프로젝트 및 라이선스

이 프로젝트는 Hyunwoong Ko의 [KoChat](https://github.com/gusdnd852/kochat)을 기반으로 제작되었습니다. 기반 코드의 라이선스는 저장소의 [LICENSE](./LICENSE)를 따릅니다.

UI에는 이주임이 제작한 [Freesentation](https://freesentation.blog/freesentation)을 사용했습니다. Freesentation은 SIL Open Font License 1.1에 따라 사용할 수 있으며, 자세한 안내는 [폰트 라이선스 문서](./demo/static/fonts/LICENSE.md)에서 확인할 수 있습니다.
